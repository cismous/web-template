# 开发说明

切换到项目目录下，可以执行以下命令:

## `pnpm install`

先安装项目依赖

## 安装本地包

由于 @skzm/web-common @skzm/web-store-common 是本地包，需要单独执行安装

`pnpm link --global @skzm/web-common`
`pnpm link --global @skzm/web-store-common`
